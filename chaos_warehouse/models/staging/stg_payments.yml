version: 2

models:
  - name: stg_payments
    description: "Cleaned and normalized payments"
    columns:
      - name: payment_id
        tests:
          - not_null
          - unique
      - name: order_id
        tests:
          - not_null